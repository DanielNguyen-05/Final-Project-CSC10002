#pragma once
#include <windows.h>
#include <sys/stat.h>
#include <string>
#include <fstream>
#include "LinkedList.hpp"

bool year_exits(std::string year);
bool create_School_year(std::string curYear);